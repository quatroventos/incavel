<IfModule mod_ssl.c>
<VirtualHost *:443>
    ServerName  onipecas.com.br
    ServerAlias www.onipecas.com.br

    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/onipecas.com.br/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/onipecas.com.br/privkey.pem
    Include /etc/letsencrypt/options-ssl-apache.conf

    SSLProxyEngine On

    ProxyPreserveHost Off
    RequestHeader unset Host
    RequestHeader set   Host "incavel.com.br"
    RequestHeader set   X-Public-Host "onipecas.com.br"
    RequestHeader set   X-Forwarded-Host "%{HTTP_HOST}s"
    RequestHeader set   X-Forwarded-Proto "https"
    RequestHeader set   X-Forwarded-For "%{REMOTE_ADDR}s"

    RewriteEngine On
    RewriteCond %{REQUEST_URI} ^/$
    RewriteRule ^/?$ https://incavel.com.br/representantes/onipecas/ [P,L]

    ProxyPassMatch ^/(.*)$ https://incavel.com.br/$1
    ProxyPassReverse / https://incavel.com.br/

    Header edit Location "^https?://incavel\.com\.br(/.*)$" "$1"
    ProxyPassReverseCookieDomain incavel.com.br onipecas.com.br
    Header edit Set-Cookie "(?i)Domain=incavel\.com\.br" "Domain=onipecas.com.br"

    AddOutputFilterByType SUBSTITUTE text/html text/css application/javascript
    Substitute "s|https://incavel.com.br/|/|ni"
    Substitute "s|http://incavel.com.br/|/|ni"

    Header always set X-Frame-Options SAMEORIGIN
    Header always set X-Content-Type-Options nosniff
    Header always set Referrer-Policy strict-origin-when-cross-origin
</VirtualHost>
</IfModule>
